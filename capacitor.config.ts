import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'es.antogonza.fotosgram',
  appName: 'FotosGram',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
